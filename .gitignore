# --- Dependencies ---
node_modules/
package-lock.json   # Optional: keep this if you want version consistency, ignore if it causes conflicts

# --- Production Build ---
/dist
dist
/build

# --- Environment Variables (SECURITY CRITICAL) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- OS System Files ---
.DS_Store       # Mac-specific junk file
Thumbs.db       # Windows-specific junk file

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*